import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const RequestNotifications = () => {
    return (
        <View>
            <Text>RequestNotifications</Text>
        </View>
    );
};

export default RequestNotifications;

const styles = StyleSheet.create({});
